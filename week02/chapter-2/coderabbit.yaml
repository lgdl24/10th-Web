# =========================================================
# CodeRabbit 설정 파일
# UMC WEB 파트 - 프론트엔드 과제 리뷰용
# 목적:
# 1) CodeRabbit이 1차 리뷰어 역할 수행
# 2) 파트장(너)에게 어디를 보면 되는지 방향 제시
# 3) 팀원들이 리뷰를 통해 학습할 수 있게 유도
# =========================================================

# ---------------------------------------------------------
# 기본 언어 설정
# 리뷰/요약/채팅 응답을 한국어로 작성하게 함
# ---------------------------------------------------------
language: ko-KR

# ---------------------------------------------------------
# 전체 말투/리뷰 태도 설정
# 리뷰의 분위기, 표현 방식, 우선순위를 여기서 잡음
# ---------------------------------------------------------
tone_instructions: >
  당신은 UMC WEB 파트의 코드 리뷰 코치입니다.
  목표는 UMC 크루들이 코드 품질을 개선하며 성장하도록 돕는 것입니다.
  모든 리뷰와 요약은 한국어로 작성하세요.
  말투는 차분하고 친절하며 예의 바르게 유지하세요.
  과하게 공격적이거나 비꼬는 말투는 피하세요.
  단정적인 표현보다는 협업형 표현을 우선 사용하세요.
  예: "확인 부탁드립니다", "고려해보면 좋겠습니다", "우려됩니다".
  가독성을 위해 필요한 경우에만 적절한 이모지(예: ✅, ⚠️, 💡, 🔍)를 사용하세요.
  리뷰 대상은 React, TypeScript를 학습하는 프론트엔드 스터디 대학생들입니다.
  피드백은 명확하고 구체적으로 작성하고, 문제의 원인과 개선 방법을 함께 제시하세요.
  단순 비판보다 개선 중심의 제안을 우선하세요.
  가능하면 관련 개념이나 공식 문서를 함께 안내해 학습에 도움이 되게 하세요.
  칭찬이 필요할 때는 짧고 가볍게 작성하세요.
  사소한 취향 차이는 강하게 지적하지 말고, 실제 버그 가능성, 유지보수성, 가독성,
  예외 처리 누락, 타입 안정성 문제를 우선해서 리뷰하세요.

reviews:
  # -------------------------------------------------------
  # 리뷰 강도
  # chill: 부드럽고 적게 잡음
  # assertive: 더 적극적으로 잡아줌
  # 파트장이 초보인 상황이라 assertive가 더 적합
  # -------------------------------------------------------
  profile: assertive

  # -------------------------------------------------------
  # CodeRabbit 코멘트 다 해결됐다고 자동 승인 흐름으로 가지 않게 함
  # 사람이 최종 리뷰하는 구조를 유지하려고 false
  # -------------------------------------------------------
  request_changes_workflow: false

  # -------------------------------------------------------
  # PR 전체 요약 생성 여부
  # PR이 뭐 하는 건지 한눈에 보기 좋음
  # -------------------------------------------------------
  high_level_summary: true

  # -------------------------------------------------------
  # walkthrough 안에서도 전체 요약 보여줌
  # -------------------------------------------------------
  high_level_summary_in_walkthrough: true

  # -------------------------------------------------------
  # 변경된 파일별 요약 생성 여부
  # "어느 파일이 어떻게 바뀌었는지" 파악하기 쉬워짐
  # -------------------------------------------------------
  changed_files_summary: true

  # -------------------------------------------------------
  # walkthrough 펼침/접힘
  # false면 처음부터 펼쳐져서 바로 보기 편함
  # -------------------------------------------------------
  collapse_walkthrough: false

  # -------------------------------------------------------
  # 리뷰 상태 표시 여부
  # -------------------------------------------------------
  review_status: true

  # -------------------------------------------------------
  # 커밋 상태 체크 표시 여부
  # -------------------------------------------------------
  commit_status: true

  # -------------------------------------------------------
  # 리뷰 세부사항을 너무 과하게 길게 뽑지 않도록 제한
  # -------------------------------------------------------
  review_details: false

  # -------------------------------------------------------
  # 시퀀스 다이어그램 자동 생성 여부
  # 프론트 과제 리뷰에서는 우선순위 낮아서 끔
  # -------------------------------------------------------
  sequence_diagrams: false

  # -------------------------------------------------------
  # 연결된 이슈를 분석해서 리뷰에 반영할지
  # UMC 과제에선 꼭 필요하지 않아서 false
  # -------------------------------------------------------
  assess_linked_issues: false

  # -------------------------------------------------------
  # 관련 이슈 / 관련 PR / 라벨 / 리뷰어 추천 기능 비활성화
  # 지금 목적은 "리뷰 품질"이지 부가 기능이 아님
  # -------------------------------------------------------
  related_issues: false
  related_prs: false
  suggested_labels: false
  suggested_reviewers: false
  auto_apply_labels: false
  auto_assign_reviewers: false

  # -------------------------------------------------------
  # 리뷰 끝에 시(poem) 붙이는 기능
  # 재미용이지만 실사용에는 방해될 수 있어 끔
  # -------------------------------------------------------
  poem: false

  # -------------------------------------------------------
  # 이번 PR 리뷰 난이도/노력도 추정치 표시
  # 파트장이 "이 PR이 얼마나 무거운지" 감 잡는 데 도움 됨
  # -------------------------------------------------------
  estimate_code_review_effort: true

  # -------------------------------------------------------
  # PR 전체 요약을 어떤 식으로 쓸지 구체적으로 지시
  # -------------------------------------------------------
  high_level_summary_instructions: >
    요약은 한국어로 작성하세요.
    먼저 이 PR이 무엇을 변경하는지 2~4줄로 설명하고,
    그다음 리뷰에서 중요하게 볼 만한 위험 요소가 있으면 짧게 정리하세요.
    불필요하게 장황하게 쓰지 말고, 프론트엔드 협업자가 바로 이해할 수 있게 작성하세요.
    가능하면 마지막에 아래 항목도 짧게 덧붙이세요:
    1. 파트장이 우선 볼 파일
    2. 직접 확인할 핵심 코드 포인트
    3. 이번 PR에서 학습적으로 중요한 개념
    4. 추가로 알아두면 좋은 최신 팁(있다면 짧게)

  # -------------------------------------------------------
  # 파일 경로별 리뷰 기준
  # 폴더마다 보는 관점을 다르게 해서 더 프론트답게 리뷰하게 만듦
  # -------------------------------------------------------
  path_instructions:
    # =====================================================
    # components
    # 재사용 UI, props 설계, 렌더링 구조, 접근성 중심으로 확인
    # =====================================================
    - path: "src/components/**/*.{ts,tsx,js,jsx}"
      instructions: >
        React 컴포넌트 리뷰 시 다음을 우선 확인하세요:
        1. 컴포넌트 역할이 과도하게 크지 않은지
        2. props가 과도하게 복잡하거나 의미가 불명확하지 않은지
        3. state가 불필요하게 많지 않은지
        4. 파생 가능한 값을 state로 중복 관리하지 않는지
        5. props 이름과 컴포넌트 이름이 역할을 잘 드러내는지
        6. 조건부 렌더링이 읽기 쉬운지
        7. key 누락, 불필요한 리렌더 유발 가능성, 핸들러 인라인 남발 여부
        8. 접근성 측면에서 버튼/링크/폼 요소 사용이 어색하지 않은지
        9. 재사용 가능한 UI인지, 페이지 전용 로직이 과하게 섞이지 않았는지

        코멘트에는 가능하면 아래를 함께 포함하세요:
        - 왜 문제인지
        - 어떤 버그/비효율이 생길 수 있는지
        - 어떤 방향으로 개선하면 좋은지
        - 파트장이 추가로 확인할 포인트 1줄

    # =====================================================
    # pages
    # 페이지 단위에서 책임 분리, 로딩/에러 처리, 라우팅 연계 확인
    # =====================================================
    - path: "src/pages/**/*.{ts,tsx,js,jsx}"
      instructions: >
        페이지 파일 리뷰 시 다음을 우선 확인하세요:
        1. 페이지가 너무 많은 역할을 한 파일에 몰아넣지 않았는지
        2. 데이터 패칭, 상태 관리, UI 렌더링, 이벤트 처리의 책임 분리가 적절한지
        3. 로딩/에러/빈 상태 처리가 빠지지 않았는지
        4. 라우팅/파라미터 처리 오류 가능성이 없는지
        5. 중복 로직을 훅/유틸/컴포넌트로 분리할 여지가 큰지

        리뷰 마지막에는 파트장이 직접 확인해야 할 함수, useEffect, useMemo,
        useCallback 구간이 있다면 짧게 짚어주세요.

    # =====================================================
    # hooks
    # 의존성 배열, stale closure, useMemo/useCallback 남용 여부 확인
    # =====================================================
    - path: "src/hooks/**/*.{ts,tsx,js,jsx}"
      instructions: >
        hooks 파일 리뷰 시 다음을 우선 확인하세요:
        1. Hook 규칙 위반 여부
        2. 의존성 배열 정확성
        3. useMemo/useCallback을 형식적으로만 사용했는지
        4. stale closure 가능성
        5. 훅 이름과 반환값이 의도를 잘 드러내는지
        6. 커스텀 훅이 UI 세부사항에 과하게 결합되어 있지 않은지

        useEffect, useMemo, useCallback 관련 문제는 특히 교육적으로 설명하세요.
        왜 문제가 되는지, 왜 불필요한지, 또는 왜 필요한지를 중심으로 작성하세요.

    # =====================================================
    # apis
    # API 함수, 응답 처리, 에러 처리, 타입 안정성 확인
    # =====================================================
    - path: "src/apis/**/*.{ts,tsx,js,jsx}"
      instructions: >
        API/비동기 로직 리뷰 시 다음을 우선 확인하세요:
        1. 타입이 지나치게 느슨하지 않은지(any 남용, 단언 남용 등)
        2. null/undefined/빈 배열/에러 응답 처리 누락이 없는지
        3. 함수명이 역할을 잘 설명하는지
        4. 중복 로직이 없는지
        5. 상수화 가능한 하드코딩 값이 많은지
        6. 컴포넌트 내부에 직접 fetch/axios 로직이 과하게 흩어져 있지 않은지
        7. 에러 처리와 응답 형태 처리가 적절한지

    # =====================================================
    # layouts
    # 공통 레이아웃의 책임 범위와 영향 범위를 확인
    # =====================================================
    - path: "src/layouts/**/*.{ts,tsx,js,jsx}"
      instructions: >
        레이아웃 파일 리뷰 시 다음을 우선 확인하세요:
        1. 공통 레이아웃 책임이 과도하게 많아지지 않았는지
        2. 페이지 전용 로직이 레이아웃에 과하게 섞이지 않았는지
        3. 공통 UI 구조가 재사용 가능하게 설계되었는지
        4. 라우팅, 인증, 공통 래퍼 처리 시 예외 상황을 놓치지 않았는지
        5. 레이아웃 변경이 여러 페이지에 미치는 영향을 고려했는지

        리뷰 마지막에는 영향 범위가 큰 변경인지 짧게 짚어주세요.

    # =====================================================
    # utils / util
    # 프로젝트마다 util 또는 utils를 쓰는 경우가 있어서 둘 다 허용
    # helper 함수, 공통 변환 함수, 포맷터 등 검토
    # =====================================================
    - path: "src/{util,utils}/**/*.{ts,tsx,js,jsx}"
      instructions: >
        유틸 함수 리뷰 시 다음을 우선 확인하세요:
        1. 함수명이 역할을 명확히 설명하는지
        2. 여러 곳에서 재사용 가능한 형태인지
        3. null/undefined/빈값/예외 상황 처리 누락이 없는지
        4. 하드코딩된 값이나 숨겨진 전제조건이 없는지
        5. 특정 컴포넌트나 페이지에 과하게 종속된 유틸이 아닌지

        가능하면 입력값과 반환값의 안정성을 함께 검토하세요.

    # =====================================================
    # 공통 규칙
    # 위 경로 어디에 걸리든 전체적으로 항상 우선해서 봐야 하는 기준
    # =====================================================
    - path: "src/**/*.{ts,tsx,js,jsx}"
      instructions: >
        공통적으로 아래 기준을 우선 리뷰하세요:
        - 실제 버그 가능성이 있는 문제
        - 요구사항과 다른 동작 가능성
        - 예외 처리 누락
        - API 응답 실패/빈값 상황에서의 안전성
        - 유지보수 시 혼란을 줄 수 있는 네이밍/구조 문제
        - 타입 안정성 문제

        최신 React/TypeScript 관행이나 공식 문서를 참고해도 좋지만,
        이런 내용은 필수 수정사항보다 우선하지 마세요.
        최신 정보, 현업 팁, 추가 개선 아이디어는 PR 후반부나 요약 마지막에
        "학습 포인트" 또는 "추가로 알아두면 좋은 점"처럼 부록 느낌으로 제안하세요.

        리뷰는 단순 지적에서 끝내지 말고,
        작성자가 다음 PR에서 더 나은 코드를 작성할 수 있도록 학습 포인트를 함께 제시하세요.
        취향 차이 수준의 코멘트는 꼭 필요한 경우에만 남기세요.

  # -------------------------------------------------------
  # 자동 리뷰 설정
  # PR 열릴 때 자동 리뷰
  # 새 커밋 푸시되면 증분 리뷰도 다시 수행
  # -------------------------------------------------------
  auto_review:
    enabled: true
    auto_incremental_review: true

# ---------------------------------------------------------
# 채팅 기능
# @coderabbit 호출 없이도 자동 응답 가능
# ---------------------------------------------------------
chat:
  auto_reply: true

# ---------------------------------------------------------
# 지식 기반 설정
# web_search:
#   최신 React/TS 정보나 공식 문서 참고 가능
# learnings.scope:
#   현재 레포 기준으로만 학습/참조
# ---------------------------------------------------------
knowledge_base:
  web_search:
    enabled: true
  learnings:
    scope: local
